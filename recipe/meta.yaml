{% set name = "bioviz" %}
{% set version = "2.3.0" %}
{% set build_num = 2 %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://github.com/pyomeca/{{ name }}/archive/Release_{{ version }}.tar.gz
  sha256: 9cbd7f97f9db22e1329c1018bed96989ca5f5247eb941311000da1a83d15afac

build:
  number: {{ build_num }}
  skip: true  # [py<310]
  script: {{ PYTHON }} -m pip install . -vv
  string: py{{ py }}_h{{ PKG_HASH }}_{{ build_num }}

requirements:
  host:
    - python
    - pip
  run:
    - python
    - eigen
    - rbdl
    - biorbd
    - casadi
    - rbdl
    - biorbd
    - vtk
    - pyqt
    - pyomeca
    - packaging

test:
  imports:
    - biorbd  # [linux]
    - bioviz  # [not linux]

about:
  home: http://github.com/pyomeca/bioviz
  license: Apache-2.0
  license_file: LICENSE
  summary: Visualization toolkit for the biorbd suite
  description: |
    Biorbd visualization toolkit 
  doc_url: https://github.com/{{ name }}/{{ name }}
  dev_url: https://github.com/{{ name }}/{{ name }}

extra:
  recipe-maintainers:
    - pariterre
